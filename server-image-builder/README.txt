This just serves as a directory for local testing of the image.
It is used via the WILDFLY_BUILDER_SERVER_IMAGE_BUILDER_LOCATION environemt variable and is where the
files get manipulated, and the pom to provision the server is created. Run the maven build to provision
the server from this directory.